import Link from "next/link";

export default function Home() {
  return (
    <div className="min-h-screen bg-slate-50 text-slate-900">
      <main className="mx-auto flex min-h-screen max-w-5xl flex-col gap-8 px-6 py-12 sm:px-10">
        <section className="rounded-3xl border border-slate-200 bg-white p-10 shadow-sm">
          <h1 className="text-4xl font-semibold tracking-tight">RCI Toufik</h1>
          <p className="mt-4 max-w-2xl text-lg leading-8 text-slate-600">
            Welcome to the resort booking dashboard. Use the links below to
            navigate between pages and manage users, resorts, and bookings.
          </p>
        </section>

        <section className="grid gap-4 sm:grid-cols-2">
          {[
            { label: "Last Call Vacation", href: "/lastCallVacation" },
            { label: "Login", href: "/login" },
            { label: "Register", href: "/registration" },
            { label: "Profile", href: "/profile" },
            { label: "Dashboard", href: "/dashboard" },
            { label: "Admin Panel", href: "/admin-panel" },
          ].map((item) => (
            <Link
              key={item.href}
              href={item.href}
              className="rounded-3xl border border-slate-200 bg-slate-100 p-6 text-left transition hover:border-slate-300 hover:bg-slate-50"
            >
              <h2 className="text-2xl font-semibold">{item.label}</h2>
              <p className="mt-2 text-sm text-slate-500">
                Open the {item.label.toLowerCase()} page.
              </p>
            </Link>
          ))}
        </section>
      </main>
    </div>
  );
}
